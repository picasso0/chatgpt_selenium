version: '3.8'

services:
  selenium:
    build: .
    image: chatgpt_selenium:latest
    container_name: chatgpt_selenium
    ports:
      - "8000:8000"  # Optional: If you want to expose VNC or any other port
    volumes:
      - .:/app  # Mount current directory to /app in container for easy access
    environment:
      - MINIO_PROTOCOL=https
      - MINIO_URL=minio.sirafgroup.com
      - MINIO_ACCESS_KEY=7qMlPjxSnrQ6fKGCPpl3
      - MINIO_SECRET_KEY=zuf+RJCJY2rlmuU3WYI7ztbyvnKqq4N7bIdt1AumjA82
      - MINIO_BUCKET_NAME=chatgpt
      - RABBITMQ_URL=31.214.171.201
      - RABBITMQ_PORT=5672
      - RABBITMQ_USERNAME=gateway
      - RABBITMQ_PASSWORD=Bgateway@1256
      - RABBITMQ_QUEUE=chatgpt_selenium
      - RABBITMQ_EXCHANGE=default
      - RABBITMQ_VHOST=gateway	
      - RABBITMQ_PROMT_ROUTING_KEY=promt
      - RABBITMQ_ANSWER_ROUTING_KEY=answer
      - AUTH_JWT_SECRET_KEY=django-insecure-e-8z0#u67o-p@h6muvch)rl5p(01e^-=o*m02x&+t+fq$(3a#^
      - AUTH_JWTALGORITHM=HS256
      - MONGODB_URL=mongodb://77.238.108.86:27000/log?retryWrites=true&w=majority
      - PYTHONUNBUFFERED=1
      - DISPLAY=:99  # Set display for Xvfb if needed
